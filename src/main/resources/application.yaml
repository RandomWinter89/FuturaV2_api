spring:
  application:
    name: futuraTwo
  datasource:
    url: jdbc:postgresql://localhost:5432/futura2
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  liquibase:
    enabled: false

server:
  port: ${PORT:8080}

jwt:
  secret: X9#mK2$pL8@nQ5&rT1*wY4^uI7!oP3sA
  expiration: 86400000