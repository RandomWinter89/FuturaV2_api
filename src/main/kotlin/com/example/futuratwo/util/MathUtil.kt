package com.example.futuratwo.util

object MathUtil {

    /*
    public fun DiscountStack(): Double {
        //1 - [(1 - dn/100) * (1 - dn/100)]

        var stack: Double = 1.00

        for (i in 1..stack) {
            val percentage = 1 - (stack / 100)
            stack *= percentage
        }

        val result = 1 - stack

        return 0.00
    }
     */
}